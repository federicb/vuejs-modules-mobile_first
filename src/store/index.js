import { createStore } from 'vuex'
import productos from '../modules/productos'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    productos
  }
})
